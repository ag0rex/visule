+ apply systems on world state

+ add system init function

+ system dependencies -> system state (stored in world state)
+   e.g. put render frame in system state
    note: system state should not change  

+ move systems to separate namespaces

+ new size system
+ new move system
+ new render system

+ collision to be part of the move component

- split regen system
  - system for removal
  - system for generation

- return values for system functions
  - decide on diff / new state

- add protocol for systems

- add records for components

- event-based generation of entities
  - mutable source of events

- macro to add systems
  - would allow injection of system key

- time-based generation of entities
  + interval system
  - timeout system